{
  // share's own imports use "@syokan/app/*" (package exports); the pulled-in syokan source
  // uses relative imports only, so no path alias is needed.
  "extends": "../../tsconfig.base.json",
  "include": [
    "types.ts",
    "worker.ts",
    "worker.test.ts",
    "build.ts",
    "csp.generated.ts",
    "viewer"
  ],
  "exclude": ["node_modules", "viewer/dist"]
}
